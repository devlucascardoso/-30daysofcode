/*const lucas = {
  grossSalary: 2000,
  desconto: 0.1,
  salarioLiquido: 0,
};

function calcularDesconto(grossSalary, desconto) {
  return grossSalary - grossSalary * desconto;
}

lucas.salarioLiquido = calcularDesconto(lucas.grossSalary, lucas.desconto);

console.log(lucas.salarioLiquido);
*/


// .replace -> substitui o texto expecífico em uma string
/* EXEMPLO
var myText = 'I am a string';
var newString = myText.replace('string','texto alterado');
console.log(newString);
*/

// .join -> adiciona todos os elementos de um array em uma string, 
// separados pela string separadora especificada(' || ').
/* EXEMPLO
var myArray = ['I', 'love', 'chocolate', 'frogs'];
//var madeAString = myArray.join(' ');
var madeAString = myArray.join('');
console.log(madeAString);
*/